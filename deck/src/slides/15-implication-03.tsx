import Implication from "../components/Implication";

export default function Implication03() {
  return (
    <Implication
      number="03"
      statement="Filtering is a routine. Routines can run on their own. People keep the decision."
      possibleNow="Any stream too wide to read: technology and competitor watch, patents, supplier changes, service reports, quality events. Fetched, narrowed and proposed with a reason every morning, confirmed by a person."
      notes="Thirty seconds. Filtering one stream is the example; the point is that a recurring routine nobody has time for can run by itself every morning and hand a person a short, reasoned digest to decide on. Any stream that today is skimmed by everyone or read by nobody fits: the machine does the fetching, narrowing and proposing; the person keeps the decision."
    />
  );
}
