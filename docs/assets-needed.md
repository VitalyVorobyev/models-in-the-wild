# Asset register — v0.5

Evidence screenshots use containment by default. Native diagrams are editable React/SVG;
ScoreQuant scientific plots are generated from actual recorded states. Fonts and media are local.

## Captured or generated

| Asset | Provenance | Use |
|---|---|---|
| Atlas overview | Existing supplied `atlas-overview.webp` | Slide 8, fully contained; no clipped detail pane |
| Atlas narrative | Live public narrative, 2026-09-13, 1280×800 | Slide 10; source-backed graph is slide 9 |
| Redaktion references | Local `/#/referenzen`, 2026-09-13, 1280×800 | Slide 18: the Goethe A1 list, 95 of 95 claimed, and the note that B2 has no source |
| Deutsch-Atlas landing | Existing supplied `art-deutsch.webp` (the learner landing page: cards due, next topic) | Slides 2 and 16 |
| Radar daily digest | Actual Markdown for 2026-08-27, exported into evidence JSON | Slide 12 excerpt, not the cumulative public board |
| ScoreQuant partitions and retention | `scripts/export-scorequant.py` (data), `scripts/render-scorequant.py` (plots, deck style, Inter with a sans fallback stack) | Slide 23: 21 actual recorded states, same axes and points; one retention panel with hard train/validation and the soft objective |
| ScoreQuant hero | `art-scorequant.webp`, the docs walkthrough, cropped below the browser tab strip (996×520) | Slides 2 and 21 |
| Problem figure | `deck/src/content/problem-figure.ts`, generated: 44 Gaussian points, six seeds, their Voronoi cells, a 3×2 grid | Slide 22, illustration only |
| Audio | `ls-essen-einkaufen-01.mp3`, source provenance JSON | Slide 19: the full 32-second scene (re-exported 2026-09-13; the earlier 12-second cut stopped mid-dialogue); the transcript and the approval checklist on the slide come from the scene YAML and the provenance file |
| Family viewer | `art-family-docs.webp`, restored from `main` and cropped below the browser tab strip (996×520) | Slides 2 and 7. Vitaly cleared it on 2026-09-13: first names only, no secrets. |
| Family letter | `family-letter.webp`, the document pane cropped from the same capture (575×390) | Slide 8, the worked example. |

Audio source: adjacent Deutsch-Atlas checkout `content/listening/a1/ls-essen-einkaufen-01.mp3`.
The models named on slide 19 are the ones pinned in `tools/listening-studio/models.lock.json`.
Approval and provenance: `data/audio-provenance/a1/ls-essen-einkaufen-01.json`.
The source provenance records no reference audio or voice cloning and a human-approved master.
This existing audio is not claimed to have been generated by today's studio UI.

## Remaining capture specifications

None. A Tonwerk studio capture was wanted in v0.5 (`127.0.0.1:8765` was blocked); v0.6 builds
slide 19 as a composition of the four stations plus the approved audio instead, so no slot is
open. If Vitaly captures a populated Mischung or Prüfung scene later (no bearer tokens), it
can replace the station cards; until then nothing on the slide claims to be a studio screenshot.
`deutsch-app.webp` and `redaktion-grammar.png` were removed with the slides that used them.

## Reproduction

Run the export scripts with the adjacent ScoreQuant Python environment. Override source checkouts
with `SCOREQUANT_REPO`, `ATLAS_REPO`, `RADAR_REPO` if needed. Import media from `src/assets/` so Vite
rewrites URLs under `/models-in-the-wild/`.

## Live-demo fallback

Atlas: narrative capture plus sourced relationship diagram. Deutsch: prepared learner session
(profile onboarding otherwise appears), existing screenshot and bundled audio. No remote iframe
or model endpoint is needed for the presentation itself.
