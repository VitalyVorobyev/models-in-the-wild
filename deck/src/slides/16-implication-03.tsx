import Implication from "../components/Implication";

export default function Implication03() {
  return (
    <Implication
      number="03"
      statement="Any stream people skim or ignore can be filtered daily, with a reason attached."
      possibleNow="Technology updates, competitors, patents, supplier changes, customer feedback, service reports, quality events. A deterministic shortlist, then judgment, then a written reason."
      notes="Thirty seconds. This is about organizational information bandwidth, not programming speed. The same shape applies to any stream that today is either skimmed by everyone or read by nobody: cheap deterministic narrowing, a frontier model spent only on the shortlist, one recorded reason per decision, and a human gate before anything is published."
    />
  );
}
